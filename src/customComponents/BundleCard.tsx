interface ICardProps {
    title?: string;
    count?: number;
    data?: any;
    isLoading?: boolean;
    error?: any;
    handler?: () => void;
  }

 export const BundleCard = (props: ICardProps) => {
    // const { title, count, data, handler} = props;

    return (
        <>
        
        </>
    );
  }
  